# Secrets — never commit
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
*.egg-info/
dist/
build/

# Node / frontend
frontend/node_modules/
frontend/dist/
.npm
*.log

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Docker (local overrides)
compose.override.yml

# Runtime data mounted into the api container (supplier rules, etc.)
# User-edited per host — never source-control.
data/

# Legacy Django project (not part of this repo)
legacy/
